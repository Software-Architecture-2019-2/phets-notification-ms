version: "3"
services:
  phets-notification-db:
    image: postgres
    volumes:
      - phets-notification-db-volume:/var/lib/postgresql/data
    ports:
      - "3004:5432"
  phets-notification-ms:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 4004 -b '0.0.0.0' && rake db:create db:migrate"
    # volumes:
    #   - .:/sa_notification_ms
    ports:
      - "4004:4004"
    depends_on:
      - phets-notification-db

volumes:
  phets-notification-db-volume:
